name: artifact Many

on:
  workflow_dispatch

jobs:
   Name:
    runs-on: ubuntu-latest
    
    steps:
          - name: Multiple type
            uses: zaproxy/action-api-scan@v0.5.0
            with:
              target: 'https://www.google.com''https://reqres.in/'
              allow_issue_writing: false
              artifact_name: "Google"
   
 
